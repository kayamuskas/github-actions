name: Test Action

on:
  workflow_dispatch:

jobs:
# Build services job
  test-job:
    runs-on: ubuntu-latest
    name: 'Some test job'
    steps:
      - name: First step
        run: echo 'First step'
      - name: Second step
        shell: bash
        run: |
          echo 'Second step'
          pwd
          ls -la
          echo ''----------------------------------------------------------------------------------------------------'
          echo '${{ toJSON(github.event) }}' | jq
          echo ''----------------------------------------------------------------------------------------------------'
          echo '${{ github.ref }}'
